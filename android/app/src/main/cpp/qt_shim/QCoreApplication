#ifndef QCOREAPPLICATION_SHIM_H
#define QCOREAPPLICATION_SHIM_H

#include "QString"

class QCoreApplication {
public:
    static QString translate(const char *context, const char *key, const char *disambiguation = nullptr, int n = -1) {
        return QString(key);
    }
};

#endif
